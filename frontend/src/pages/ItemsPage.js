// src/pages/ItemsPage.js
import React from 'react'; // Giả sử bạn có component này

export default function ItemsPage() {
  return (
    <div className="container mx-auto py-8">
      <h1 className="text-4xl font-bold text-center mb-8">Tất cả Vật phẩm</h1>
      {/* Bạn có thể đặt component hiển thị tất cả vật phẩm ở đây */}
      {/* <AllItems /> */}
      <p className="text-center text-gray-400">Trang này sẽ hiển thị tất cả các vật phẩm.</p>
    </div>
  );
}